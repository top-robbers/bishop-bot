services:
  discord-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: top-robbers-discord-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3000:3000"
